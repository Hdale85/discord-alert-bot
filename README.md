# discord-alert-bot
Built an automated RSS/Atom feed monitor that posts new articles to a Discord channel via webhook. Includes duplicate detection so items are never reposted, configurable post limits to prevent spam, and rich Discord embeds with title, description, and timestamp. Runs on a cron schedule for fully hands-off monitoring. Zero external dependencies.
